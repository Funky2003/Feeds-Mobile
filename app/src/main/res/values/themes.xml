<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Feeds" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorControlHighlight">@color/hint_grey_color</item>
    </style>

    <!--  Custom theme for the launch screen  -->
    <style name="Base.Theme.Feeds.Launch" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/feeds_launch_background</item>
        <item name="colorPrimary">@color/black</item>
        <item name="colorControlHighlight">@color/hint_grey_color</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Custom search view -->
    <style name="ThemeOverlay.SearchView" parent="">
        <item name="android:editTextColor">@color/white</item>
        <item name="android:textColorHint">@color/hint_grey_color</item>
    </style>

    <!--  Custom edit text  -->
    <style name="Custom_EditTex." parent="">
        <item name="strokeWidth">5dp</item>
        <item name="background">@color/black</item>
    </style>

    <style name="Theme.Feeds" parent="Base.Theme.Feeds" />
</resources>